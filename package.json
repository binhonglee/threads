{
  "name": "threads",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "generate": "esbuild --bundle --platform=node index.ts | node",
    "serve": "esbuild --bundle --platform=node server.ts | node",
    "altimeter": "altimeter config.json"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@globetrotte/altimeter": "^0.0.3",
    "@types/node": "^17.0.35",
    "esbuild": "^0.14.42",
    "twitter-threads": "^0.0.7"
  }
}
